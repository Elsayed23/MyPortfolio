/** @type {import('next').NextConfig} */
const nextConfig = {
  // For GitHub Pages later: set `output: 'export'`, `basePath`, and `assetPrefix`
  // to match the repository name, then deploy the `out` directory.
};

export default nextConfig;